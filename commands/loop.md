---
description: "Start the experiment loop: PROPOSE → MUTATE → BENCHMARK → ANNOTATE"
argument-hint: "[--repo <path>] [--max-runs N]"
---

Invoke the `research-loop:loop` skill and start the experiment loop with args: $ARGUMENTS
